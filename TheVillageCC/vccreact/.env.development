# Development environment configuration
# This file configures the React dev server to proxy API requests to the IIS backend

# URL of the IIS backend server
# Default is localhost:8080 (common IIS port)
# Change this to match your local IIS configuration (e.g., http://localhost:80, http://localhost:5000)
REACT_APP_API_URL=http://localhost:5000
